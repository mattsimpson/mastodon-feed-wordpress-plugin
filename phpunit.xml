<?xml version="1.0" encoding="UTF-8"?>
<phpunit
    bootstrap="tests/bootstrap.php"
    backupGlobals="false"
    colors="true"
    convertErrorsToExceptions="true"
    convertNoticesToExceptions="true"
    convertWarningsToExceptions="true"
    verbose="true"
>
    <testsuites>
        <testsuite name="unit">
            <directory suffix=".php">tests/php/unit</directory>
        </testsuite>
        <testsuite name="integration">
            <directory suffix=".php">tests/php/integration</directory>
        </testsuite>
    </testsuites>

    <coverage processUncoveredFiles="true">
        <include>
            <directory suffix=".php">./</directory>
        </include>
        <exclude>
            <directory>./tests</directory>
            <directory>./vendor</directory>
            <directory>./node_modules</directory>
            <directory>./build</directory>
            <directory>./src</directory>
            <directory>./templates</directory>
            <file>./uninstall.php</file>
        </exclude>
    </coverage>

    <php>
        <!-- WP_TESTS_DIR and WP_CORE_DIR are auto-detected in tests/bootstrap.php -->
        <!-- You can override them here if needed for your environment -->
    </php>
</phpunit>
